.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install gpass

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv gpass
    $ pip install gpass
